中文 | [EN](/CONTRIBUTING_EN.md)

# 贡献指南

感谢您对倒计时日历工具项目的关注和支持！我们非常欢迎社区贡献，无论是修复bug、添加新功能还是改进文档。

## 📝 贡献流程

### 1. 环境准备

1. **Fork 仓库**
   - 访问 [项目仓库](<仓库地址>)
   - 点击右上角的 "Fork" 按钮创建自己的副本

2. **克隆仓库**
   ```bash
   git clone <您的fork地址>
   cd Countdown-Calendar
   ```

3. **创建分支**
   - 为您的修改创建一个新分支
   ```bash
   git checkout -b feature/功能名称
   # 或
   git checkout -b fix/问题描述
   ```

### 2. 代码规范

请遵循以下代码规范，确保项目代码风格一致：

- **HTML**
  - 使用语义化标签
  - 缩进使用 2 个空格
  - 属性值使用双引号

- **CSS**
  - 选择器使用小写字母加连字符
  - 属性名冒号后加空格
  - 每条规则后加分号
  - 使用 CSS 变量管理颜色和尺寸

- **JavaScript**
  - 使用驼峰命名法
  - 缩进使用 2 个空格
  - 语句末尾加分号
  - 使用 ES6+ 语法
  - 避免全局变量，使用 IIFE 封装模块
  - 添加必要的注释说明复杂逻辑

### 3. 开发与测试

1. **本地测试**
   - 在浏览器中打开 `index.html` 测试您的修改
   - 确保功能正常，无控制台错误
   - 测试响应式布局，确保在不同设备上显示正常

2. **功能测试**
   - 测试核心功能：添加/编辑/删除倒计时
   - 测试主题切换功能
   - 测试正计时和倒计时模式
   - 测试时间单位自定义功能

### 4. 提交代码

1. **检查修改**
   - 确保只修改了必要的文件
   - 检查是否有未提交的临时文件

2. **提交更改**
   - 编写清晰的提交信息
   ```bash
   git add .
   git commit -m "feat: 添加新功能描述"  # 新功能
   # 或
   git commit -m "fix: 修复问题描述"      # 修复bug
   # 或
   git commit -m "docs: 更新文档描述"     # 更新文档
   ```

3. **推送到远程**
   ```bash
   git push origin feature/功能名称
   ```

### 5. 创建 Pull Request

1. **打开 Pull Request**
   - 访问您的 fork 仓库
   - 点击 "Pull requests" 标签
   - 点击 "New pull request" 按钮

2. **填写信息**
   - 选择正确的分支
   - 填写标题，简洁明了地描述您的修改
   - 在描述中详细说明：
     - 您修改的内容
     - 为什么要做这个修改
     - 如何测试您的修改
     - 相关的 Issue（如果有）

3. **提交 Pull Request**
   - 点击 "Create pull request" 按钮
   - 等待维护者审核

## 🐛 报告问题

如果您发现了 bug 或有功能建议，请按照以下步骤报告：

1. **搜索现有 Issue**
   - 先查看 [Issue 列表](<仓库地址>/issues)，确认问题是否已被报告

2. **创建新 Issue**
   - 点击 "New issue" 按钮
   - 选择合适的模板（Bug 报告或功能请求）
   - 填写详细信息：
     - 问题描述
     - 复现步骤
     - 预期行为
     - 实际行为
     - 浏览器信息
     - 截图（如果有）

## 📞 联系方式

如果您有任何疑问，可以通过以下方式联系我们：

- **GitHub Issues**：在仓库中创建 Issue
- **邮件**：<开发者邮箱>（如果提供）

## 🌟 行为准则

请尊重其他贡献者，保持友好和专业的沟通。我们致力于创建一个包容、多元的社区环境。

---

感谢您的贡献，让我们一起让这个项目变得更好！
